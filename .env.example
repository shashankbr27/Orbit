# ORBIT runs fully offline/local-first — no secrets required for the MVP.
# These are placeholders for the future cloud layer (see README > Future cloud).
# NEXT_PUBLIC_SUPABASE_URL=
# NEXT_PUBLIC_SUPABASE_ANON_KEY=
